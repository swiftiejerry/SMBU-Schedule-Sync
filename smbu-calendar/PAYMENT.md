# 聚合支付方案（一码双付 · 防漏单 · 钱怎么到账）

> 回答三个问题：具体怎么实现？2026 年最安全的现代化方案？钱怎么到你账上？

## 一、先说钱怎么到你账上（这决定技术选型）

**合规是前置条件，绕不开：**

| 路径 | 前提 | 结算到哪 | 评价 |
|---|---|---|---|
| **A. 个体工商户 + 官方商户号** | 个体工商户执照（深圳可线上办，成本低） | 微信/支付宝直结你的**银行账户**，T+1 | ✅ **推荐**。费率 ~0.6%/0.55%，钱不过第三方手 |
| B. 个人收款码 + 金额轮询 | 无 | 个人零钱 | ❌ 违反微信/支付宝 ToS，风控封码是常态，**商用必死** |
| C. "个人免签"聚合平台（虎皮椒类） | 无 | 平台 → 你 | ⚠️ 灰色：资金过第三方手，跑路/冻结风险，二清嫌疑 |
| D. 公司主体 + 商户号 | 公司 | 对公账户 | 重，现阶段不必 |

**结论：走 A**。深圳个体工商户线上可办；有了执照，微信支付商户号 + 支付宝商家
账户都能申请（学生本人可办）。**所有资金直清你的银行卡，不经任何中间人** ——
这既是最安全（无第三方卷款风险），也是最便宜（官方费率）。

## 二、"一码双付"的正确实现（不需要特殊资质）

**原理**：二维码不是支付链接，而是**你自己域名的一个页面**。用户用微信/支付宝扫一扫，
App 内置浏览器打开该页 → 前端按 `navigator.userAgent` 识别扫码来源 →
跳转到对应渠道已创建的支付链接。

```
你的页面  https://pay.你的域名/o/{order_id}
   ├─ UA 含 MicroMessenger → 展示微信 Native 支付二维码/调起 JSAPI
   └─ UA 含 AlipayClient   → 跳支付宝手机网站支付链接
```

- 两个渠道**各自**用你的商户号下单（微信 Native 下单得 `code_url`，支付宝预下单得支付链接）
- 两个渠道**共享同一个 `order_id`**（我们账本天然支持：订单先建，渠道后挂）
- 用户实际只完成一笔；若同时发起，靠账本的 `trade_no` 全局去重兜底

> 所谓"聚合"，**本质是这层 UA 路由 + 统一订单模型**，不需要买任何聚合服务。
> 商户号审核通过后，两个渠道各接一次（微信 Native ~1 天，支付宝当面付/手机网站 ~1 天）。

## 三、防漏单（已实现，接入真实渠道即可用）

代码在 `app/payments/`，三道防线 + 测试 9/9：

1. **持久化**：订单落 SQLite（WAL + `synchronous=FULL`），重启/断电不丢单
2. **幂等**：按**网关 trade_no** 全局去重（`processed_trades` 表），重复回调只入账一次；
   同一 trade 不能结算两笔订单
3. **对账**：每分钟 `reconcile()` 拉网关已结算列表，回调丢了也能补单；
   连"已被过期但网关确认已付"的订单都能救回（`allow_expired`）

接入真实渠道时的硬性规则（写进代码 review checklist）：
- 回调验签 **HMAC-SHA256**（已实现，`SMBU_PAY_CALLBACK_SECRET` 未配置 = 全拒）
- **金额服务端说了算**：下单时锁定金额，回调金额不符直接拒（防篡改 1 分钱支付）
- 处理渠道的**主动补偿**：微信/支付宝会重试通知（15s/15s/30s/3m/10m/20m/30m/30m/30h/3h…），
  我们的幂等保证重复通知无害
- 每日对账单（微信/支付宝都提供）与本地账本核对 —— 对账任务的最终兜底

## 四、支付接口的安全设计（对齐 SECURITY.md）

- 回调端点：验签 + 金额校验 + 幂等，三关全过才动账本
- 前端**永远不传价格**：价格只存在于服务端订单（`SMBU_PAYMENT_CENTS`）
- 商户密钥只走环境变量，**绝不进代码/镜像/git**（`.dockerignore` 已排除 `.env`）
- 支付接口与课表接口同源同域，同样吃 CSP/限流/审计

## 五、接入步骤（当你办好商户号）

1. 定价：`SMBU_PAYMENT_CENTS=xxx`（分）
2. `SMBU_PAY_CALLBACK_SECRET=$(openssl rand -hex 32)` 进 `.env`
3. 实现 `app/payments/gateway.py` 的真实 Adapter（继承 `PaymentGateway`，
   覆写 `create_aggregate_code` / `verify_callback` / `list_settled_trades` —— 接口已定）
4. `SMBU_PAYMENTS_ENABLED=true` 上线，先灰度 3-5 个真实订单
5. 跑一轮支付压测（回调并发 + 幂等 + 对账）后再全量
